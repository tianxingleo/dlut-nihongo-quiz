# 默认所有文本文件用 LF；Windows 用户签出时也保持 LF，
# 避免 CRLF/LF 混乱导致 git diff 一片警告。
* text=auto eol=lf

# 二进制文件明确不进行行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg -text
*.woff binary
*.woff2 binary
*.pdf binary
